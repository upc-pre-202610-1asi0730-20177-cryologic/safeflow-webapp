# Build de producción (Netlify, Azure Static Web Apps, etc.).
# API desplegada en Azure App Service (sin barra final).
VITE_API_BASE_URL=https://safeflow-api-chbaa6fxbbdja6b2.southeastasia-01.azurewebsites.net

# Rutas por defecto del backend real (api/inventory/items, api/logistics/..., etc.).
# No uses los slugs de MockAPI (inventory-items, logistics-destinos, ...).
